<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Fleet Operations Tracker | Forgot Password</title>
  <link rel="stylesheet" href="/css/form_style.css" />
</head>
<body>

<header>
    <div class="logo">
      <img src="/img/logo.jpg" alt="Team Logo" />
      <h1>Fleet Operations Tracker</h1>
    </div>
    <nav>
      <% if (!User) { %>
          <!-- When NOT logged in -->
          <a href="/login" class="auth-link active">Login</a>
          <a href="/auth/google" class="hero-btn google-btn">Sign in with Google</a>
          <a href="/signup" class="auth-link">Register</a>
      <% } else { %>
          <!-- When logged in -->
          <span class="user-name">Welcome, <%= User.fullName %></span>
          <a href="/" class="<%= activePage === 'home' ? 'active' : '' %>">Home</a>
          <a href="/create" class="<%= activePage === 'create' ? 'active' : '' %>">Log New Trip</a>
          <a href="/trucks" class="<%= activePage === 'trucks' ? 'active' : '' %>">View Active Trips</a>
          <a href="/logout" class="auth-link logout">Logout</a>
      <% } %>
    </nav>
</header>

<main class="page-content">
  <div class="form-container sign-up-form">

    <h2>Reset Your Password</h2>
    <p>Enter your account email. A reset link will be sent if the account exists.</p>

    <% if (error) { %>
      <div class="message-box error-box"><%= error %></div>
    <% } %>

    <form action="/forgot-password" method="POST">

      <div class="form-group">
        <label>Email Address <span class="required-star">*</span></label>
        <input type="email" name="email" placeholder="email@company.com" required />
      </div>

      <div class="form-group" style="text-align: right; padding-top: 15px;">
        <button type="submit" class="submit-btn primary">Find Account</button>
      </div>

      <p class="form-footer">
        <a href="/login">Back to Login</a>
      </p>

    </form>
  </div>
</main>

<footer>
  <p>&copy; 2025 Fleet Operations Tracker Project. INFR3120.</p>
</footer>

</body>
</html>